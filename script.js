// This file can be used for any interactive JavaScript elements.
// For a simple landing page, it might not have much content initially.

const initializePage = () => {
    console.log('PowerUp Gym Landing Page Loaded!');
    // Add any future event listeners or dynamic content here
};

// Ensure the DOM is fully loaded before running our script
document.addEventListener('DOMContentLoaded', initializePage);
